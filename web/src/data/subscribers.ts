import type { Subscriber } from "@/types";

/**
 * The subscriber roster starts empty — every record comes from a real signup
 * made through the site (checkout) or added manually from the admin console.
 */
export const SEED_SUBSCRIBERS: Subscriber[] = [];
